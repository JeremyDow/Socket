/**
 * Markdown tool entry point (compatibility wrapper).
 * Implementation remains in src/workflows/transcript/markdown-renderer.js.
 */

export {
  renderTranscriptMarkdown,
  formatRangeTimestamp,
  describeRange,
  isQuoteClip,
} from '../../workflows/transcript/markdown-renderer.js';