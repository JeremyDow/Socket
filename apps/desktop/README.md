# Socket Desktop Shell (placeholder)

Reserved for a future desktop host around the Socket workspace. The current
validated runtime is the Node server in the repository root (`server.js`).

Do not add build artifacts or machine-local configuration to Git.